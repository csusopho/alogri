[Problem: 1914. 循环轮转矩阵](https://leetcode.cn/problems/cyclically-rotating-a-grid/description/)

### 方法：模拟

由于 $m$ 和 $n$ 都是偶数，矩阵可以完全分解为若干个 **矩形环**。第 $0$ 层是最外圈，第 $1$ 层是次外圈，依此类推。层数共有 $min(m, n)/ 2$ 层。

在某一圈内，每个元素沿逆时针方向移动 $k$ 个位置。如何处理逆时针旋转？

如果我们把这一圈的元素按逆时针顺序 **“拉直”** 成一个一维数组，那么逆时针旋转 $k$ 次就等同于将这个数组 **循环右移 $k$ 位**。

拿示例 $1$ 进行举例。

- 第一步，逆时针取出元素，得到 $[10,40,30,20]$；
- 第二步，循环右移一位，得到 $[20,10,40,30]$；
- 第三步，所有元素逆时针放回去。

切记，取出的顺序必须和放回的顺序 **一致**！

也就是说，如果我们逆时针按圈取出时，首先提取的是上边，那么，放回去的时候，也必须先填充上边！从上边的最右侧元素开始提取，必须也从该位置开始填充！

> $ps$：当然，也可以按照顺时针提取一维数组。放回去的时候，也是按照顺时针。和上面的做法仅仅是顺序不同，代码我放在评论区。

注意，每一层的周长 $L$ 是不同的。旋转 $k$ 次的效果等同于旋转 $k \pmod L$ 次。由于指定的 $k$ 可能非常大（高达 $10^9$），所以一定记得 **取模优化**！

**本质**： $2D\to 1D\to 2D$。

**总结**：遍历每一层提取元素，计算实际有效旋转步数，旋转，填回矩阵。

代码如下，已附加注释：

```Python
# python
class Solution:
    def rotateGrid(self, grid: List[List[int]], k: int) -> List[List[int]]:
        m, n = len(grid), len(grid[0])
        # 层数
        num_layers = min(m, n) // 2
        
        # 逐层处理
        for i in range(num_layers):
            # 当前层的边界
            top, left = i, i
            bottom, right = m - 1 - i, n - 1 - i
            
            # 按逆时针顺序提取当前层的元素
            layer = []
            
            # 左侧边 (从上到下)
            for r in range(top, bottom):
                layer.append(grid[r][left])
            # 下侧边 (从左到右)
            for c in range(left, right):
                layer.append(grid[bottom][c])
            # 右侧边 (从下到上)
            for r in range(bottom, top, -1):
                layer.append(grid[r][right])
            # 上侧边 (从右到左)
            for c in range(right, left, -1):
                layer.append(grid[top][c])
            
            # 实际旋转次数
            length = len(layer)
            real_k = k % length
            if real_k == 0:
                continue
            
            # 循环右移
            rotated_layer = layer[-real_k:] + layer[:-real_k]
            
            # 写回原位置
            idx = 0
            # 必须遵循与提取时完全一致的顺序
            for r in range(top, bottom):  # 左
                grid[r][left] = rotated_layer[idx]
                idx += 1
            for c in range(left, right):  # 下
                grid[bottom][c] = rotated_layer[idx]
                idx += 1
            for r in range(bottom, top, -1):  # 右
                grid[r][right] = rotated_layer[idx]
                idx += 1
            for c in range(right, left, -1):  # 上
                grid[top][c] = rotated_layer[idx]
                idx += 1
        
        return grid
```

```Java
// java
class Solution {
    public int[][] rotateGrid(int[][] grid, int k) {
        int m = grid.length, n = grid[0].length;
        // 层数
        int numLayers = Math.min(m, n) / 2;

        // 逐层处理
        for (int i = 0; i < numLayers; i++) {
            // 当前层的边界
            int top = i, left = i;
            int bottom = m - 1 - i, right = n - 1 - i;

            // 按逆时针顺序提取当前层的元素
            List<Integer> layer = new ArrayList<>();

            // 左侧边 (从上到下)
            for (int r = top; r < bottom; r++) {
                layer.add(grid[r][left]);
            }
            // 下侧边 (从左到右)
            for (int c = left; c < right; c++) {
                layer.add(grid[bottom][c]);
            }
            // 右侧边 (从下到上)
            for (int r = bottom; r > top; r--) {
                layer.add(grid[r][right]);
            }
            // 上侧边 (从右到左)
            for (int c = right; c > left; c--) {
                layer.add(grid[top][c]);
            }

            // 实际旋转次数
            int length = layer.size();
            int realK = k % length;
            if (realK == 0) continue;

            // 写回原位置
            int idx = 0;
            // 必须遵循与提取时完全一致的顺序
            for (int r = top; r < bottom; r++) { // 左
                grid[r][left] = layer.get((idx + length - realK) % length);
                idx++;
            }
            for (int c = left; c < right; c++) { // 下
                grid[bottom][c] = layer.get((idx + length - realK) % length);
                idx++;
            }
            for (int r = bottom; r > top; r--) { // 右
                grid[r][right] = layer.get((idx + length - realK) % length);
                idx++;
            }
            for (int c = right; c > left; c--) { // 上
                grid[top][c] = layer.get((idx + length - realK) % length);
                idx++;
            }
        }

        return grid;
    }
}
```

- 时间复杂度： $O(m\times n)$，其中 $m,n$ 是矩阵 $grid$ 的行数/列数，每个元素访问三次
- 空间复杂度： $O(m+n)$，表示临时列表 $layer$ 的大小

如果本篇题解你觉得写得不错，欢迎点个赞让更多人看见~

> 题解已发布力扣平台 [我的题解](https://leetcode.cn/problems/cyclically-rotating-a-grid/solutions/3965569/mo-ni-la-zhi-cheng-yi-wei-li-qing-xia-bi-36ii/)
