[Problem: 2784. 检查数组是否是好的](https://leetcode.cn/problems/check-if-array-is-good/description/)

### 方法：哈希

$base[n]$ 是一个长度为 $n + 1$ 的数组，其特点为：

1. **包含的数字范围**：从 $1$ 到 $n$。
2. **出现频率**：
   - 数字 $1, 2, \dots, n-1$ 各出现 **1 次**。
   - 数字 $n$ 出现 **2 次**。
3. **数组长度**：如果数组长度为 $L$，那么则有 $L = n + 1$。

要判断 $nums$ 是否为 “好数组”，就是需要验证以上三个条件：

- 设数组长度为 $L$，则 $n = L - 1$。
- 所有数字必须在 $[1, n]$ 之间。
- 数字 $n$ 必须恰好出现 $2$ 次，其余数字恰好出现 $1$ 次。

判断数字的出现次数，因此使用 **哈希表** 最为方便。

此外，本题也可以对 $nums$ 排序，直接与目标序列对比，逻辑最直观。这里不再展示。

代码如下，已附加注释：

```Python
# python
class Solution:
    def isGood(self, nums: List[int]) -> bool:
        n = len(nums) - 1
        
        # 统计每个数字出现的次数
        cnt = {}
        for x in nums:
            cnt[x] = cnt.get(x, 0) + 1
        
        # 验证 [1, n-1] 出现的次数是否为 1
        for i in range(1, n):
            if cnt.get(i) != 1:
                return False
        
        # 验证 n 出现的次数是否为 2
        if cnt.get(n) != 2:
            return False
        
        # 总长度正确，则不会有冗余数字
        if len(cnt) != n:
            return False
        
        return True
```

```Java
// java
class Solution {
    public boolean isGood(int[] nums) {
        int n = nums.length - 1;
        
        // 统计每个数字出现的次数
        Map<Integer, Integer> cnt = new HashMap<>();
        for (int x : nums) {
            cnt.put(x, cnt.getOrDefault(x, 0) + 1);
        }
        
        // 验证 [1, n-1] 出现的次数是否为 1
        for (int i = 1; i < n; i++) {
            if (cnt.getOrDefault(i, 0) != 1) {
                return false;
            }
        }
        
        // 验证 n 出现的次数是否为 2
        if (cnt.getOrDefault(n, 0) != 2) {
            return false;
        }
        
        // 总长度正确，则不会有冗余数字
        if (cnt.size() != n) {
            return false;
        }
        
        return true;
    }
}
```

- 时间复杂度： $O(n)$，其中 $n$ 是数组 $nums$ 的长度，两次遍历
- 空间复杂度： $O(n)$，表示哈希表 $cnt$ 的大小

如果本篇题解你觉得写得不错，欢迎点个赞让更多人看见~

> 题解已发布力扣平台 [我的题解](https://leetcode.cn/problems/check-if-array-is-good/solution/ha-xi-jian-dan-ti-jian-dan-zuo-by-pricel-2uvp/)
