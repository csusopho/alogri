[Problem: 1674. 使数组互补的最少操作次数](https://leetcode.cn/problems/minimum-moves-to-make-array-complementary/description/)

### 方法：差分

最终，每一对对称的数字之和都等于 $X$。注意，数字的范围在 $1$ 到 $limit$ 之间！

设一对数字为 $(a, b)$，假设 $a \le b$（如果 $a > b$，交换它们不影响结果）。对于这一对数字，如果要使它们的和等于 $X$，需要多少次操作？

我们需要观察 $X$ 在不同区间时，操作次数的变化：

1）**操作 0 次**：当 $X = a + b$ 时，不需要任何操作。

2）**操作 1 次**：

可以把 $a$ 变成 $1 \dots limit$ 中的任意数，或者把 $b$ 变成 $1 \dots limit$ 中的任意数。

- 如果只改 $a$：和的范围变成 $[1+b, limit+b]$。
- 如果只改 $b$：和的范围变成 $[a+1, a+limit]$。

取两者的并集，只要 $X$ 落在区间 $[\min(a, b) + 1, \max(a, b) + limit]$ 内（且 $X \neq a+b$），就只需要 **1 次** 操作。

3）**操作 2 次**：

如果 $X$ 不在上述范围内，即使修改一个数也无法达到 $X$，则需要修改两个数。修改两个数后，和的范围是 $[1+1, limit+limit]$，即 $[2, 2 \times limit]$。

所以，如果 $X$ 在 $[2, \min(a, b)]$ 或 $[max(a, b) + limit + 1, 2 \times limit]$ 范围内，需要 **2 次** 操作。

---

注意，每个数对的目标和都是 $X$。

如果暴力枚举每一个可能的 $X$（从 $2$ 到 $2 \times limit$），然后遍历所有对，计算总操作数，时间复杂度是 $O(limit \times n)$，这会超时。

我们需要一种高效的方法来统计每个 $X$ 对应的总操作数。由于每对数字对 $X$ 的贡献是 **分段** 的，所以可以使用 **差分数组**。

对于每一对 $(a, b)$（设 $a \le b$）：

- 在区间 $[2, 2 \times limit]$ 上，默认初始操作数都加 $2$。
- 在区间 $[a + 1, b + limit]$ 上，操作数减少 $1$（即从 $2$ 变成 $1$）。
- 在点 $X = a + b$ 上，操作数再减少 $1$（即从 $1$ 变成 $0$）。

最后，对差分数组求 **前缀和**，得到每个 $X$ 对应的总操作数，取最小值即可。

为什么想到差分？

如果我们把 $X$ 看作坐标轴，每一对数字都在坐标轴上不同的区间贡献了不同的数值。固定 $X$ 后，就是计算每对数字的贡献，也就变为 “区间加减” 问题。

差分的具体原理/推导，可以看这篇 [题解](https://leetcode.cn/problems/number-of-students-doing-homework-at-a-given-time/solutions/2899892/chai-fen-mei-ju-chai-fen-yuan-li-ying-yo-pbel/)，或者这篇 [Wiki百科](https://oi-wiki.org/basic/prefix-sum/#%E5%B7%AE%E5%88%86)。

代码如下，已附加注释：

```Python
# python
class Solution:
    def minMoves(self, nums: List[int], limit: int) -> int:
        n = len(nums)
        # 差分数组
        diff = [0] * (2 * limit + 2)
        
        for i in range(n // 2):
            a, b = nums[i], nums[n - 1 - i]
            
            # 确保 a <= b 方便计算区间
            if a > b:
                a, b = b, a
            
            # 在 [2, 2*limit] 区间内，默认所有 X 都需要 2 次操作
            diff[2] += 2
            diff[2 * limit + 1] -= 2
            
            # 在 [a + 1, b + limit] 区间内，只需要 1 次操作
            diff[a + 1] -= 1
            diff[b + limit + 1] += 1
            
            # 在 a + b 这个点上，只需要 0 次操作
            diff[a + b] -= 1
            diff[a + b + 1] += 1
        
        # 遍历差分数组求前缀和，寻找最小值
        ans = n
        cur_moves = 0
        
        for x in range(2, 2 * limit + 1):
            cur_moves += diff[x]
            if cur_moves < ans:
                ans = cur_moves
        
        return ans
```

```Java
// java
class Solution {
    public int minMoves(int[] nums, int limit) {
        int n = nums.length;
        // 差分数组
        int[] diff = new int[2 * limit + 2];
        
        for (int i = 0; i < n / 2; i++) {
            int a = nums[i];
            int b = nums[n - 1 - i];
            
            // 确保 a <= b 方便计算区间
            if (a > b) {
                int temp = a;
                a = b;
                b = temp;
            }
            
            // 在 [2, 2*limit] 区间内，默认所有 X 都需要 2 次操作
            diff[2] += 2;
            diff[2 * limit + 1] -= 2;
            
            // 在 [a + 1, b + limit] 区间内，只需要 1 次操作
            diff[a + 1] -= 1;
            diff[b + limit + 1] += 1;
            
            // 在 a + b 这个点上，只需要 0 次操作
            diff[a + b] -= 1;
            diff[a + b + 1] += 1;
        }
        
        // 遍历差分数组求前缀和，寻找最小值
        int ans = n;
        int curMoves = 0;
        
        for (int x = 2; x <= 2 * limit; x++) {
            curMoves += diff[x];
            if (curMoves < ans) {
                ans = curMoves;
            }
        }
        
        return ans;
    }
}
```

- 时间复杂度： $O(n + limit)$，其中 $n$ 是数组 $nums$ 的长度
- 空间复杂度： $O(limit)$，表示差分数组 $diff$ 的大小

如果本篇题解你觉得写得不错，欢迎点个赞让更多人看见~

> 题解已发布力扣平台 [我的题解](https://leetcode.cn/problems/minimum-moves-to-make-array-complementary/solutions/3967762/chai-fen-guan-cha-gui-lu-zhu-yi-fan-wei-17v8w/)
