[Problem: 3660. 跳跃游戏 IX](https://leetcode.cn/problems/jump-game-ix/description/)

### 方法：前后缀

简化一下两个跳跃规则：

1. 向 **右** 跳到一个比当前值 **小** 的数。
2. 向 **左** 跳到一个比当前值 **大** 的数。

如果下标 $i$ 能够跳到下标 $j$，那么下标 $j$ 是否能跳回下标 $i$ 呢？

- 假设 $j > i$ 且 $nums[j] < nums[i]$，满足规则 $1$，可以 $i \to j$。
- 因为 $i < j$ 且 $nums[i] > nums[j]$，满足规则 $2$，所以 $j \to i$ 也是合法的。

**结论**：这个跳跃关系是 **双向** 的。如果两个位置之间存在跳跃路径，它们属于同一个 **连通分量**。在同一个连通分量中，所有下标都可以 **互相到达**。因此，对于下标 $i$，它能到达的最大值就是它所属连通分量中的 **最大元素值**。

这里我不再举例，具体可以看看示例 $2$。

**如何划分连通分量？**

需要找到数组中的 “割裂点”。如果我们将数组在某个位置 $k$ 分成左半部分 $[0 \dots k]$ 和右半部分 $[k+1 \dots n-1]$，什么情况下左边永远无法跳到右边？

根据规则：

- **左跳右**：需要左边的数 > 右边的数。
- **右跳左**：需要左边的数 > 右边的数。

反过来说，如果 **左半部分的所有数都小于等于右半部分的所有数**，那么这两部分之间就绝对不可能发生跳跃。也即，如果 $\max(nums[0 \dots k]) \le \min(nums[k+1 \dots n-1])$，则 $k$ 和 $k+1$ 之间是断开的。

如何快速计算子数组中的最值？**前后缀**。

**定义** $preMax[i]$ 表示 $nums[0...i]$ 中的最大值，以及 $sufMin[i]$ 表示 $nums[i...n-1]$ 中的最小值。

遍历数组，如果满足 $preMax[i] \le sufMin[i+1]$，说明 $i$ 是一个分块的结束。对于每一个独立的块，该块内所有 $ans[i]$ 都等于该块中的最大值。

> $ps$：切记，上面的不等式中是 **小于等于**，而不是 **小于**！否则会卡在用例 $989/1002$，具体为 $[11,18,11]$，别问我怎么知道的(wuwu)。

题目要求 “从 $i$ 出发能到达的最大值”，实际上就是求 **“在满足 $nums[a] > nums[b] (a < b)$ 这一连接条件下”** 的连通分量中，每个分量的最大值是多少。

**为什么这个逻辑是严谨的**？

如果一个块 $[L, R]$ 无法再被细分，说明在该块内部，不存在任何一个位置 $k$ 满足 $\max(L \dots k) \le \min(k+1 \dots R)$。这意味着，对于块内任何一个切分点，左边总能找到一个比右边某个数大的数。这种 “逆序对” 关系像胶水一样把整个块粘在一起，使得块内所有下标都可以通过跳跃互达。一旦互达，它们能到达的最大值自然就是块内的全局最大值。

代码如下，已附加注释：

```Python
# python
class Solution:
    def maxValue(self, nums: List[int]) -> List[int]:
        n = len(nums)
        
        # 前缀最大值
        pre_max = [0] * n
        cur_max = float('-inf')
        for i in range(n):
            cur_max = max(cur_max, nums[i])
            pre_max[i] = cur_max
        
        # 后缀最小值
        suf_min = [0] * n
        cur_min = float('inf')
        for i in range(n - 1, -1, -1):
            cur_min = min(cur_min, nums[i])
            suf_min[i] = cur_min
        
        ans = [0] * n
        start = 0
        
        # 寻找分块边界
        for i in range(n):
            # 数组末尾，或者满足断开条件
            if i == n - 1 or pre_max[i] <= suf_min[i+1]:
                # [start, i] 构成一个连通块
                block_max = pre_max[i]  # 该块内的最大值
                
                # 填充
                for j in range(start, i + 1):
                    ans[j] = block_max
                # 下一块的起点
                start = i + 1
        
        return ans
```

```Java
// java
class Solution {
    public int[] maxValue(int[] nums) {
        int n = nums.length;
        
        // 前缀最大值
        int[] preMax = new int[n];
        int curMax = Integer.MIN_VALUE;
        for (int i = 0; i < n; i++) {
            curMax = Math.max(curMax, nums[i]);
            preMax[i] = curMax;
        }
        
        // 后缀最小值
        int[] sufMin = new int[n];
        int curMin = Integer.MAX_VALUE;
        for (int i = n - 1; i >= 0; i--) {
            curMin = Math.min(curMin, nums[i]);
            sufMin[i] = curMin;
        }
        
        int[] ans = new int[n];
        int start = 0;
        
        // 寻找分块边界
        for (int i = 0; i < n; i++) {
            // 数组末尾，或者满足断开条件
            if (i == n - 1 || preMax[i] <= sufMin[i + 1]) {
                // [start, i] 构成一个连通块
                int blockMax = preMax[i];  // 该块内的最大值
                
                // 填充
                for (int j = start; j <= i; j++) {
                    ans[j] = blockMax;
                }
                // 下一块的起点
                start = i + 1;
            }
        }
        
        return ans;
    }
}
```

- 时间复杂度： $O(n)$，其中 $n$ 是数组 $nums$ 长度，虽然代码中有嵌套循环，但是指针 $i$ 只增不减，实际每个元素被访问线性次
- 空间复杂度： $O(n)$，表示数组 $preMax,sufMin$ 的大小，返回值 $ans$ 一般不计入复杂度

如果本篇题解你觉得写得不错，欢迎点个赞让更多人看见~

> 题解已发布力扣平台 [我的题解](https://leetcode.cn/problems/jump-game-ix/solutions/3964390/qian-hou-zhui-xun-zhao-gui-lu-ben-zhi-sh-fzar/)
