[Problem: 1665. 完成所有任务的最少初始能量](https://leetcode.cn/problems/minimum-initial-energy-to-finish-tasks/description/)

### 方法：贪心

对于任务 $x$，它的初始能量要求是 $8$，花费能量 $3$，说明剩下的能量最少是 $5$。如果存在另一个任务 $y$，它要求初始能量 $4$，那就说明可以后完成 $y$！

我们发现，“差值” 非常重要。差值越大，代表该任务在实际消耗之外，对需要 **额外预留的最低能量净值**（剩余能量）的要求越高，越早做越有利！

**贪心策略：** 计算每个任务的“差值” $d_i = minimum_i - actual_i$。按照 $d_i$ **从大到小** 的顺序排列任务，然后依次执行，即可得到最少的初始能量。

---

如何保证这种 **贪心是最优解**？

假设有两个任务 $T_1 = [a_1, m_1]$ 和 $T_2 = [a_2, m_2]$。

如果要完成这两个任务，有两种排序可能：

**情况 A：先做 $T_1$，再做 $T_2$**

1. 开始 $T_1$ 前，能量 $E \ge m_1$。
2. 完成 $T_1$ 后，能量变为 $E - a_1$。
3. 开始 $T_2$ 前，剩余能量必须满足 $E - a_1 \ge m_2$，即 $E \ge a_1 + m_2$。

综合以上两个条件，先 $T_1$ 后 $T_2$ 需要的初始能量 $E_A = \max(m_1, a_1 + m_2)$。

**情况 B：先做 $T_2$，再做 $T_1$**

同理，需要的初始能量 $E_B = \max(m_2, a_2 + m_1)$。

我们希望选择 $E$ 更小的方案。假设我们希望 $E_A < E_B$，即：

$$
\max(m_1, a_1 + m_2) < \max(m_2, a_2 + m_1)
$$

为了方便比较，将 $m_i$ 改写为 $a_i + (m_i - a_i)$。令 $d_i = m_i - a_i$：

- $E_A = \max(a_1 + d_1, a_1 + a_2 + d_2)$
- $E_B = \max(a_2 + d_2, a_2 + a_1 + d_1)$

对比观察两式，根据数学知识可知，显然 $a_1 + a_2 + d_2 > a_2 + d_2$，且 $a_1 + a_2 + d_1 > a_1 + d_1$。

因此，在绝大多数情况下（只要 $a_i > 0$）比较 $E_A$ 和 $E_B$ 实际上是在比较：

$$
a_1 + a_2 + d_2 \quad \text{vs} \quad a_1 + a_2 + d_1
$$

如果要 $E_A < E_B$，则需要 $a_1 + a_2 + d_2 < a_1 + a_2 + d_1$，即 **$d_2 < d_1$**。

**结论：** 差值 $d_i = (m_i - a_i)$ 越大的任务，越应该先放在前面执行。

上面是正向思路推导，逆向思路推导就是 **邻项交换法**。本质流程相同，这里不再赘述，具体推导我放在评论区，感兴趣的小伙伴自行阅读。

代码如下，已附加注释：

```Python
# python
class Solution:
    def minimumEffort(self, tasks: List[List[int]]) -> int:
        # 按照差值，从大到小排序
        # 差值越大，代表“剩余能量”的要求越高，越早做越有利
        tasks.sort(key=lambda x: x[1] - x[0], reverse=True)
        
        cur_energy = 0  # 当前拥有的能量
        ans = 0         # 累计需要的初始能量
        
        for actual, minimum in tasks:
            if cur_energy < minimum:
                # 当前能量不足以开始任务，需要补充初始能量
                diff = minimum - cur_energy
                ans += diff
                cur_energy += diff
            
            # 执行任务，消耗能量
            cur_energy -= actual
        
        return ans
```

```Java
// java
class Solution {
    public int minimumEffort(int[][] tasks) {
        // 按照差值，从大到小排序
        // 差值越大，代表“剩余能量”的要求越高，越早做越有利
        Arrays.sort(tasks, (a, b) -> (b[1] - b[0]) - (a[1] - a[0]));
        
        int cur_energy = 0;  // 当前拥有的能量
        int ans = 0;         // 累计需要的初始能量
        
        for (int[] task : tasks) {
            int actual = task[0];
            int minimum = task[1];
            
            if (cur_energy < minimum) {
                // 当前能量不足以开始任务，需要补充初始能量
                int diff = minimum - cur_energy;
                ans += diff;
                cur_energy += diff;
            }
            
            // 执行任务，消耗能量
            cur_energy -= actual;
        }
        
        return ans;
    }
}
```

- 时间复杂度： $O(n\times logn)$，其中 $n$ 是数组 $tasks$ 的长度，快排
- 空间复杂度： $O(1)$，忽略排序的 $O(logn)$ 栈开销

如果本篇题解你觉得写得不错，欢迎点个赞让更多人看见~

> 题解已发布力扣平台 [我的题解](https://leetcode.cn/problems/minimum-initial-energy-to-finish-tasks/solutions/3967164/tan-xin-ji-suan-chai-zhi-zheng-xiang-shu-xyec/)
