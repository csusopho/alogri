[Problem: 2770. 达到末尾下标所需的最大跳跃次数](https://leetcode.cn/problems/maximum-number-of-jumps-to-reach-the-last-index/description/)

### 方法：DP

目标是寻找从起点到终点的 “最长路径”，可以将 “路径长度” 定义为跳跃的次数。

我们需要记录到达数组中每个位置时，所能获得的最大跳跃次数。

**定义** $dp[i]$ 表示从下标 $0$ 跳跃到下标 $i$ 所需的最大跳跃次数。

初始位置在下标 $0$，此时跳跃次数为 $0$。所以 $dp[0] = 0$。数组其余位置初始化为 $-1$，表示最初都不可达。

为了计算 $dp[j]$，需要考虑所有在 $j$ 之前的下标 $i$。如果满足以下两个条件，我们就可以从 $i$ 跳到 $j$：

1. 位置 $i$ 是 **可达的**：即 $dp[i] \not= -1$
2. 满足 **跳跃条件**：即 $-target\le nums[j] - nums[i]\le target$

如果满足条件，则到达 $j$ 的跳跃次数可以更新为：

$$
dp[j] = \max(dp[j], dp[i] + 1)
$$

对于当前的 $j$，遍历所有可能的 $i$ 使得 $dp[j]$ 达到最大。

最终答案就是 $dp[n - 1]$。如果 $dp[n - 1]$ 仍然是 $-1$，说明无法到达终点。

代码如下，已附加注释：

```Python
# python
class Solution:
    def maximumJumps(self, nums: List[int], target: int) -> int:
        n = len(nums)
        dp = [-1] * n
        # 起点跳跃次数为 0
        dp[0] = 0
        
        # 枚举终点
        for j in range(1, n):
            # 枚举起点
            for i in range(j):
                # 检查是否可以从 i 跳到 j
                if dp[i] != -1 and abs(nums[j] - nums[i]) <= target:
                    # 更新
                    dp[j] = max(dp[j], dp[i] + 1)
        
        # 到达最后一个位置
        return dp[n - 1]
```

```Java
// java
class Solution {
    public int maximumJumps(int[] nums, int target) {
        int n = nums.length;
        int[] dp = new int[n];
        Arrays.fill(dp, -1);
        // 起点跳跃次数为 0
        dp[0] = 0;
        
        // 枚举终点
        for (int j = 1; j < n; j++) {
            // 枚举起点
            for (int i = 0; i < j; i++) {
                // 检查是否可以从 i 跳到 j
                if (dp[i] != -1 && Math.abs(nums[j] - nums[i]) <= target) {
                    // 更新
                    dp[j] = Math.max(dp[j], dp[i] + 1);
                }
            }
        }
        
        // 到达最后一个位置
        return dp[n - 1];
    }
}
```

- 时间复杂度： $O(n^2)$，其中 $n$ 是数组 $nums$ 的长度，双层 $for$ 循环
- 空间复杂度： $O(n)$，表示数组 $dp$ 的大小

如果本篇题解你觉得写得不错，欢迎点个赞让更多人看见~

> 题解已发布力扣平台 [我的题解](https://leetcode.cn/problems/maximum-number-of-jumps-to-reach-the-last-index/solutions/3966269/po-su-dp-jian-dan-tui-dao-rong-yi-li-jie-kxz3/)
