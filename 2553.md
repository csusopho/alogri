[Problem: 2553. 分割数组中数字的数位](https://leetcode.cn/problems/separate-the-digits-in-an-array/description/)

### 方法：模拟

本题是想保持原有的顺序，将数组 “展平” 到数位级别。

对任意正整数 $n$，要按原始顺序获得它的数位，有 **两种** 常见方法：

**方法一：字符串转换**：

将 $n$ 转为字符串，例如 $10921\to "10921"$。然后，遍历字符串中的每个字符，转回整数，按顺序加入结果列表。

这种代码直观、易于理解，且天然保持数位顺序（从左到右）。

**方法二：数学取模**：

用一个循环：每次 $n \% 10$ 获取最低位，然后 $n / 10$ 去除最低位。

这样得到的数位是逆序的（从低位到高位），需要再反转，或者用栈暂存后再按正确顺序输出。实现略复杂，不如字符串法直观。

这里采用最简单的方法一。

代码如下，已附加注释：

```Python
# python
class Solution:
    def separateDigits(self, nums: List[int]) -> List[int]:
        res = []
        
        for num in nums:
            # 转为字符串，从左到右提取
            for ch in str(num):
                res.append(int(ch))
        
        return res
```

```Java
// java
class Solution {
    public int[] separateDigits(int[] nums) {
        // 先计算总位数
        int totalLen = 0;
        for (int num : nums) {
            totalLen += Integer.toString(num).length();
        }
        
        int[] res = new int[totalLen];
        int idx = 0;
        
        for (int num : nums) {
            // 转为字符串，从左到右提取
            String s = Integer.toString(num);
            for (int i = 0; i < s.length(); i++) {
                res[idx++] = Character.getNumericValue(s.charAt(i));
            }
        }
        
        return res;
    }
}
```

- 时间复杂度： $O(D)$，其中 $D$ 是数组 $nums$ 所有数字的总位数
- 空间复杂度： $O(logM)$，表示临时串 $s$ 的大小，其中 $M$ 是数组 $nums$ 中的最大数字，返回值 $res$ 一般不计入复杂度

如果本篇题解你觉得写得不错，欢迎点个赞让更多人看见~

> 题解已发布力扣平台 [我的题解](https://leetcode.cn/problems/separate-the-digits-in-an-array/solutions/3966591/mo-ni-jian-dan-zuo-zhuan-hua-zi-fu-chuan-vv0g/)
